# Base URL of the CoverSignal FastAPI backend.
# Copy this file to `.env` to override (defaults to http://localhost:8000).
# Leave empty so the built SPA calls /api on the same origin (Railway).
VITE_API_BASE_URL=http://localhost:8000
