[pytest]
# Run from the project root so `backend` and `tests` import cleanly.
pythonpath = .
testpaths = tests
python_files = test_*.py
addopts = -ra
markers =
    slow: end-to-end tests that decode real videos and run ML models (deselect with -m "not slow")
